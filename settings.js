// SETTINGS

module.exports = (function () {

	// For production set false
	var DEBUG = true;

	// Port where the app will run
	var PORT = 3000;

	return {

		DEBUG : DEBUG,
		PORT  : PORT
	
	};

})();